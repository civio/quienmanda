<!--start: row-fluid -->
<div class="row-fluid">

  <!--start: Wrapper-->
  <div id="wrapper" class="full span8">

      <!-- start: Flexslider -->
      <div class="slider">
        <div id="flex1" class="flexslider home">
          <ul class="slides">

            <% @highlights.each do |post| %>
            <li>
              <%= link_to post do %>
                <%= image_tag(post.photo.file.url(:full)) %>
              <% end %>
              <div class="slide-caption n">
                <%= link_to post do %>
                  <h3><%= post.title %></h3>
                <% end %>
                <p>
                  <%= post.lead %>
                </p>
                <p>
                  <%= render 'shared/show_photo_credit', photo: post.photo %>
                </p>
              </div>
            </li>
            <% end %>

          </ul>
        </div>
      </div>
      <!-- end: Flexslider -->

  </div>
  <!-- end: Wrapper  -->  

  <!--start: Wrapper-->
  <div id="wrapper" class="full span4">

    <div id="homepage-lead">
      <h3>Participa</h3>
      <p>Envíanos imágenes que muestren vínculos entre empresarios y políticos para nuestro fotomandón o sugiere historias interesantes. Escríbenos a <a href="mailto:contacto@civio.es">contacto@civio.es</a>.</p>
      <p>Además, puedes ayudarnos a seguir investigando y aportando datos fiables con una donación.</p>
      <hr>
      <h3>¿Pero qué es esto?</h3>
      <p>
        Para curiosos (esos son los buenos): ¿Qué es Quién Manda? ¿Cómo se ha hecho? ¿Quiénes estamos detrás? Y, lo más importante, ¿por qué hacemos esto?
      </p>
    </div>

  </div>
  <!-- end: Wrapper  -->  

</div>
<!--end: row-fluid -->

<!--start: Wrapper-->
<div id="wrapper">
  
  <!-- start: Portfolio -->
  <div id="wall" class="row-fluid">
            
    <% @posts.each do |post| %>
      <%= render '/shared/show_post', :item => post, :width => 'span6', :frontpage => 'true' %>
    <% end %>

  </div>
  <!-- end: Portfolio -->
  
  <%= paginate @posts %>

</div>
<!-- end: Wrapper  -->


<script>
$(function () {

  $('#flex1').flexslider();
  
});
</script>